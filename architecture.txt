==========================================================================================
                                 ARCHITECTURE DIAGRAM
==========================================================================================

  HOST / PUBLIC NETWORK
    │
    ├── Port 8080 ────────────────────────┐
    ├── Port 5001 ──────────────────┐     │
    ├── Port 5432 ────────────┐     │     │
    ├── Port 9090 ──────┐     │     │     │
    └── Port 3000 ──┐   │     │     │     │
                    │   │     │     │     │
────────────────────┼───┼─────┼─────┼─────┼───────────────────────────────────────────────
  DOCKER CONTAINERS │   │     │     │     │
                    │   │     │     │     │
                    │   │     │     │   ┌─▼───────────────────┐
                    │   │     │     │   │      fe (UI)        │
                    │   │     │     │   │  (build: ./frontend)│
                    │   │     │     │   └─────────┬───────────┘
                    │   │     │     │             │
                    │   │     │     │             │ [depends_on: service_healthy]
                    │   │     │     │             ▼
                    │   │     │   ┌─▼───────────────────┐
                    │   │     │   │     be (API)        │
                    │   │     │   │  (build: ./backend) │
                    │   │     │   └─────────┬───────────┘
                    │   │     │             │
                    │   │     │             │ [depends_on: service_healthy]
                    │   │     │             ▼
                    │   │   ┌─▼─────────────────────┐
                    │   │   │          db           │ ◄─── [init.sql (bind mount)]
                    │   │   │  (postgres:17-alpine) │
                    │   │   └───────────┬───────────┘
                    │   │               │
                    │   │               ▼
                    │   │       [ pg-data (Volume) ]
                    │   │
                    │   │
                    │ ┌─▼─────────────────────┐
                    │ │      prometheus       │ ◄─── [config & alerts (bind mounts)]
                    │ │ (prom/prometheus:v2)  │
                    │ └─────────┬─────────────┘
                    │           │
                    │           ▼
                    │   [ prom-data (Volume) ]
                    │
              ┌─────▼─────────────────┐
              │        grafana        │
              │  (grafana/grafana:10) │ ◄─── [provisioning (bind mount)]
              └─────────┬─────────────┘
                        │
                        ▼
               [ grafana-data (Vol) ]

────────────────────────────────────────────────────────────────────────────────────────
  DOCKER NETWORKS
                                    ┌──────────────────────┐
                                    │  postgres-exporter   │
                                    └────┬────────────┬────┘
                                         │            │
                                         │            │
  [app-net] ─────────────────────────────┴────────────┼─────────────────────────────────
    ├── fe                                            │
    ├── be                                            │
    ├── db                                            │
    └── postgres-exporter                             │
                                                      │
  [monitoring-net] ───────────────────────────────────┴─────────────────────────────────
    ├── prometheus
    ├── grafana
    └── postgres-exporter

==========================================================================================
                                  TRAFFIC & DATA FLOW
==========================================================================================

1. Request Flow      : Client ──► fe:8080 ──► be:5001 ──► db:5432
2. Monitoring Flow  : grafana:3000 ──► prometheus:9090
                      prometheus:9090 ──► postgres-exporter ──► db:5432
                      (Prometheus scrapes metrics from postgres-exporter over monitoring-net)