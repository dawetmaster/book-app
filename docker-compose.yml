services:
  fe:
    build:
      context: "./frontend"
      dockerfile: "Dockerfile"
    ports:
      - 8080:8080
  be:
    build:
      context: "./backend"
      dockerfile: "Dockerfile"
    ports:
      - 5001:5001
    