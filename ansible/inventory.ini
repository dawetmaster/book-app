[webservers]
# Ganti IP di bawah ini dengan IP atau domain dari server target Anda
server_target ansible_host=192.168.1.100 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[webservers:vars]
# Lokasi direktori instalasi aplikasi di server target
app_dir=/opt/book-tracker